// File generated by SBML-to-PRISM converter
// Original file: /Users/lukas/Lab/GitHub_Repositories/sbml-test-suite/cases/stochastic/00025/00025-sbml-l3v2.xml
// @GeneticLogicLab

ctmc

// const int MAX_AMOUNT = ADD VALUE 

// Compartment size

// Model parameters
const double Alpha = 10.0; // 
const double Mu = 0.1; // 

// Species _X
// const int _X_MAX = MAX_AMOUNT;
module _X

  // _X : [0.._X_MAX] init 0;
  _X  :  int init 0;

		// Immigration
		[Immigration] _X >= 0 -> (_X'=_X+1);
		// Death
		[Death] _X > 0 -> (_X'=_X-1);

endmodule

// Species Source
// const int Source_MAX = MAX_AMOUNT;
module Source

  // Source : [0..Source_MAX] init 0;
  Source  :  int init 0;

		// Immigration
		[Immigration] Source > 0 -> (Source'=Source-1);

endmodule

// Species Sink
// const int Sink_MAX = MAX_AMOUNT;
module Sink

  // Sink : [0..Sink_MAX] init 0;
  Sink  :  int init 0;

		// Death
		[Death] Sink >= 0 -> (Sink'=Sink+1);

endmodule

// Reaction rates
module reaction_rates

		// Immigration:  ->  _X 
		[Immigration]  Alpha > 0 -> (Alpha) : true;

		// Death:  ->  Sink 
		[Death]  (Mu *_X) > 0 -> ((Mu *_X)) : true;

endmodule

// Reward structures (one per species)
// Reward 1: _X
rewards "_X" true : _X; endrewards
// Reward 2: Source
rewards "Source" true : Source; endrewards
// Reward 3: Sink
rewards "Sink" true : Sink; endrewards
