// File generated by SBML-to-PRISM converter
// Original file: /Users/lukas/Lab/GitHub_Repositories/sbml-test-suite/cases/stochastic/00032/00032-sbml-l3v2.xml
// @GeneticLogicLab

ctmc

// const int MAX_AMOUNT = ADD VALUE 

// Compartment size

// Model parameters
const double k1 = 0.001; // 
const double k2 = 0.01; // 

// Species _P
// const int _P_MAX = MAX_AMOUNT;
module _P

  // _P : [0.._P_MAX] init 100;
  _P  :  int init 100;

		// Dimerisation
		[Dimerisation] _P > 1 -> (_P'=_P-2);
		// Disassociation
		[Disassociation] _P >= 0 -> (_P'=_P+2);

endmodule

// Species P2
// const int P2_MAX = MAX_AMOUNT;
module P2

  // P2 : [0..P2_MAX] init 0;
  P2  :  int init 0;

		// Dimerisation
		[Dimerisation] P2 >= 0 -> (P2'=P2+1);
		// Disassociation
		[Disassociation] P2 > 0 -> (P2'=P2-1);

endmodule

// Reaction rates
module reaction_rates

		// Dimerisation:  ->  P2 
		[Dimerisation]  ((k1 *_P * (_P - 1)) / 2) > 0 -> (((k1 *_P * (_P - 1)) / 2)) : true;

		// Disassociation:  ->  _P 
		[Disassociation]  (k2 *_P2) > 0 -> ((k2 *_P2)) : true;

endmodule

// Reward structures (one per species)
// Reward 1: _P
rewards "_P" true : _P; endrewards
// Reward 2: P2
rewards "P2" true : P2; endrewards
