// File generated by SBML-to-PRISM converter
// Original file: /Users/lukas/Lab/GitHub_Repositories/sbml-test-suite/cases/stochastic/00027/00027-sbml-l3v2.xml
// @GeneticLogicLab

ctmc

// const int MAX_AMOUNT = ADD VALUE 

// Compartment size

// Model parameters
const double k = 2.0; // 
const double k = 1.0; // 
const double k = 0.1; // 

// Species _X
// const int _X_MAX = MAX_AMOUNT;
module _X

  // _X : [0.._X_MAX] init 0;
  _X  :  int init 0;

		// Immigration
		[Immigration] _X >= 0 -> (_X'=_X+1);
		// Death
		[Death] _X > 0 -> (_X'=_X-1);

endmodule

// Reaction rates
module reaction_rates

		// Immigration:  ->  _X 
		[Immigration]  k > 0 -> (k) : true;

		// Death:  ->  
		[Death]  (k *_X) > 0 -> ((k *_X)) : true;

endmodule

// Reward structures (one per species)
// Reward 1: _X
rewards "_X" true : _X; endrewards
