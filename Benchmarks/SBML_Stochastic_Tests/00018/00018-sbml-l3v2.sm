// File generated by SBML-to-PRISM converter
// Original file: /Users/lukas/Lab/GitHub_Repositories/sbml-test-suite/cases/stochastic/00018/00018-sbml-l3v2.xml
// @GeneticLogicLab

ctmc

// const int MAX_AMOUNT = ADD VALUE 

// Compartment size
const double Cell = 0.5;

// Model parameters
const double Lambda = 0.1; // 
const double Mu = 0.11; // 

// Species _X
// const int _X_MAX = MAX_AMOUNT;
module _X

  // _X : [0.._X_MAX] init 100;
  _X  :  int init 100;

		// Birth
		[Birth] _X > 0 -> (_X'=_X-1);
		// Death
		[Death] _X > 0 -> (_X'=_X-1);

endmodule

// Reaction rates
module reaction_rates

		// Birth:  ->  _X 
		[Birth]  (Cell * Lambda *_X) > 0 -> ((Cell * Lambda *_X)) : true;

		// Death:  ->  
		[Death]  (Cell * Mu *_X) > 0 -> ((Cell * Mu *_X)) : true;

endmodule

// Reward structures (one per species)
// Reward 1: _X
rewards "_X" true : _X; endrewards
